[package]
name = "web"
version = "0.1.0"
authors = ["Mykola Basov <git@mbasov.com>"]
edition = "2018"

[dependencies]
conduit = { path = "../conduit" }
server = { path = "../server" }

thiserror = "1.0"
askama = "0.8"
futures = "0.3"
pulldown-cmark = "0.8"
